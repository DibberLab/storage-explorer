services:
  storage-explorer:
    build: .
    container_name: storage-explorer
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8420:8420"
    volumes:
      - /:/hostfs:rw
